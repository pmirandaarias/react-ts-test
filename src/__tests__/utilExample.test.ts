import utilExample from '../utilExample';

describe('asd', () => {
  it('asdas', () => {
    expect(utilExample(5)).toBeFalsy();
  });
  it('asdas', () => {
    expect(utilExample(2)).toBeTruthy();
  });
});
